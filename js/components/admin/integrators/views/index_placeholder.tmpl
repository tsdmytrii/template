<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">${lang.integrator.placeholders}</h3>
    </div>

    <div id="integratorPlaceholdersBody" class="panel-body">
        <div id="placeholdersWrapper">

            <div id="addPlaceholder" class="btn btn-primary"><i class="glyphicon glyphicon-plus"></i> ${lang.placeholder.add}</div>

            <table id="placeholdersTable" class="table table-hover table-striped">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>${lang.placeholder.name}</th>
                </tr>
                </thead>
                <tbody id="placeholdersTableBody">

                </tbody>
            </table>

            <div class="clear"></div>

        </div>
    </div>
</div>
