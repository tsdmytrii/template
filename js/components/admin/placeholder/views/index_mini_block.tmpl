<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">${lang.integrator.miniBlocks}</h3>
    </div>

    <div class="panel-body">
        <div id="miniBlocksWrapper">

            <div id="addMiniBlockButton" class="btn btn-primary"><i class="glyphicon glyphicon-plus"></i> ${lang.miniBlock.add}</div>

            <table id="miniBlocksTable" class="table table-hover table-striped">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>${lang.miniBlock.name}</th>
                </tr>
                </thead>
                <tbody id="miniBlocksTableBody">

                </tbody>
            </table>

            <div class="clear"></div>

        </div>
    </div>
</div>