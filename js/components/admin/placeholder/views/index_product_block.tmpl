<div class="panel panel-default">

    <div class="panel-heading">
        <h3 class="panel-title">${lang.integrator.productBlocks}</h3>
    </div>

    <div class="panel-body">
        <div id="productBlocksWrapper">

            <div id="addProductBlockButton" class="btn btn-primary"><i class="glyphicon glyphicon-plus"></i> ${lang.productBlock.add}</div>

            <table id="productBlocksTable" class="table table-hover table-striped">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>${lang.productBlock.name}</th>
                </tr>
                </thead>
                <tbody id="productBlocksTableBody">

                </tbody>
            </table>

            <div class="clear"></div>

        </div>
    </div>


</div>